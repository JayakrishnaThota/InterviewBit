//O(n) and O(1)
public class Solution {
	public int removeElement(ArrayList<Integer> nums, int val) {
	    int index = 0;
	    for(int n:nums){
	        if(n!=val) nums.set(index++, n);
	    }
	    return index;
	}
}
